interface:
  display_name: "Gitcrawl"
  short_description: "Search local NodoAssist issue and PR history before live GitHub triage"
  default_prompt: "Use $gitcrawl to inspect NodoAssist issue and PR history, find related threads and duplicate candidates, then verify actionable decisions with live GitHub."
