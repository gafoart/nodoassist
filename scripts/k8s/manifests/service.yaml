apiVersion: v1
kind: Service
metadata:
  name: nodoassist
  labels:
    app: nodoassist
spec:
  type: ClusterIP
  selector:
    app: nodoassist
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
